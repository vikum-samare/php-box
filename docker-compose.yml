
version: "3"

services:

  site_1:
    image: php:7.2.2-apache
    volumes:
              - ./www/:/var/www/html/
    expose:
      - 80
    restart: always
    environment:
      VIRTUAL_HOST: www.vikum.xyz

    container_name: vikum_site

networks:
  default:
    external:
      name: nginx-proxy
